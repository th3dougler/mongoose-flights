<%- include('../partials/head') %>
<div class="card">
  <div class="card-content">
    <span class="lighten-1"><h3><%= entry.airline %></h3></span>
    <span class="card-title"><h2><%= entry.airport %></h2></span>
    <ul>
      <li>Flight No:<%= entry.flightNo%></li>
      <li>Departs: <%= new Date(entry.departs).toDateString()%></li>
    </ul>
  </div>
  <div class="card-action">
    <a class="btn" href="/my_db/<%= entry._id %>/edit">Edit</a>
    <form action="/my_db/<%= entry._id %>?_method=DELETE" method="POST">
      <button class="btn" href="/my_db/<%= entry._id %>">Delete</button>
    </form>
  </div>
</div>
<%- include('../partials/foot') %>
